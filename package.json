{
  "name": "pilkarzyki",
  "version": "0.1.0",
  "description": "Piłkarzyki na kartce — mobilna gra z reklamami AdMob (Capacitor + Android).",
  "private": true,
  "scripts": {
    "sync": "cap sync",
    "open:android": "cap open android",
    "build:android": "cap sync android && cd android && ./gradlew assembleRelease",
    "bundle:android": "cap sync android && cd android && ./gradlew bundleRelease"
  },
  "dependencies": {
    "@capacitor/android": "^6.1.2",
    "@capacitor/app": "^6.0.1",
    "@capacitor/core": "^6.1.2",
    "@capacitor/haptics": "^6.0.1",
    "@capacitor/splash-screen": "^6.0.2",
    "@capacitor/status-bar": "^6.0.1",
    "@capacitor-community/admob": "^6.3.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.1.2"
  }
}
